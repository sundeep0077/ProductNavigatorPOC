import { OptimizedRouteProductDTO } from './OptimizedRouteProductDTO';

export class RouteSavings {
    originalRouteTime: number;
    optimizedRouteTime: number;
    timeSaved: number;
    optimizedRouteProductDTO: OptimizedRouteProductDTO[];
}
